Key scvi compatibility issues:

_CONSTANTS.X_KEY is deprecated in newer scvi — replaced by REGISTRY_KEYS
LossRecorder API has changed
scvi_setup_dict_ access pattern is outdated
BaseModuleClass import paths have shifted
auto_move_data decorator behavior changed